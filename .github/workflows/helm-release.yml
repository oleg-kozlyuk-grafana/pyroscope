name: helm-release

on:
  push:
    branches:
      - main
      - "release-[0-9]+.[0-9]+"

jobs:
  call-update-helm-repo:
    permissions:
      contents: "write"
      id-token: "write"
      packages: "write"
    uses: grafana/helm-charts/.github/workflows/update-helm-repo.yaml@24bcd88fe72d0a683583fffa96867804ec02b98d
    with:
      charts_dir: operations/pyroscope/helm/
      cr_configfile: operations/pyroscope/helm/cr.yaml
      ct_configfile: operations/pyroscope/helm/ct.yaml
    secrets:
      vault_repo_secret_name: grafana-pyroscope-releases
